import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";
import { Layout } from "../../components/Layout";

export const WebSpeech: React.FC = () => {
  return <Layout title="Web speech"></Layout>;
};
