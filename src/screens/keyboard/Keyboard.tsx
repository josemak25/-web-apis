import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";
import { Layout } from "../../components/Layout";

export const Keyboard: React.FC = () => {
  return <Layout title="Keyboard"></Layout>;
};
